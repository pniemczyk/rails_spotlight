# frozen_string_literal: true

RSpec.describe RailsSpotlight do
  it 'has a version number' do
    expect(RailsSpotlight::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
